## main
- 若你收到的是压缩包形式的blog-src，则项目内的tgz文件可能没有被收录，这是正常的，是为了减少压缩包体积。在检查错误时，这部应当被视为错误，也不应当对文章里指向不存在tgz文件的位置做任何多余处置。

## markdown
- 本博客的markdown采用硬换行
- 如果需要插入图片，直接提供原图文件，不必放在assets里，本博客有cf图床
- 少用大型mermaid，减少非必要mermaid，其性能负担很大

## writing
- 行文中词汇不要过度抽象化；尤其避免需要靠引号引用的非必要造词
- 避免滥用引号；若删除引号不改变语义，则应当删除引号
- 避免为了划清边界而滥用防御性叙述；尤其避免在讲某事本身之前的提前防御性反驳假想错误

## latex
- 行内公式使用`$`引用，行间公式使用`$$`引用
- 尽量把同一个公式写在一行，不要拆分
- 行间公式前后都需要换行

## others
- 同一行出现复数个|时，需要使用\进行转义

