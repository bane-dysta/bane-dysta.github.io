## main
- 若你收到的是压缩包形式的blog-src，则项目内的tgz文件可能没有被收录，这是正常的，是为了减少压缩包体积。在检查错误时，这不应当被视为错误，也不应当对文章里指向不存在tgz文件的位置做任何多余处置。
- 每一次回答中，你提供的文件名不能重复。若你第一轮回复提供A.patch，则后续对话中不得再使用A.patch作为文件名，否则我只能拿到最开始的A.patch，而无法得到最新版。

## Requirement State
- **后续指令对先前需求的修改、否定或撤回应当视为当前需求状态的更新，而不是新的附加要求。** 当新指令与旧要求冲突时，以新指令为准；被撤销、否定、纠正或确认属于误解的内容，不再属于当前有效需求。
- 最终产物应当基于**当前有效需求**自然形成。除非项目维护者明确要求说明修改历史，否则不得在代码、命名、注释、测试、软件用户可见文本、文档、commit/PR标题或描述等最终产物中保留无实际意义的纠错痕迹。
- 尤其不得因为项目维护者曾明确说“不要X”，就在最终产物中刻意写成“无X版”“不包含X”“移除X”等；如果X本就不属于最终需求，应当像从未要求过X一样处理。
- 上述“不保留无意义纠错痕迹”的规则不妨碍在与项目维护者的开发交流和最终交付说明等位置记录**实际发生过的代码变更**；但被Agent误解、项目维护者随即纠正且从未构成有效实现的内容，不应被包装成一次功能变更。

## markdown
- 本博客的markdown采用硬换行
- 本博客有cf图床。如果需要插入图片，应直接提供原图文件，而不必放在assets里。需要插入图片的位置只需留下![](文件名)占位即可。
- 少用大型mermaid，减少非必要mermaid，其性能负担很大

## writing
- 行文中词汇不要过度抽象化；尤其避免需要靠引号引用的非必要造词
- 避免滥用引号；若删除引号不改变语义，则应当删除引号
- 避免为了划清边界而滥用防御性叙述；尤其避免在讲某事本身之前的提前防御性反驳假想错误
- 修改文章时，禁止出现修改后的文本反驳修改前的文本的情况。修改前的文本应当视作已被消灭、从未存在过；任何针对其的反驳都是无价值的；你应当确保读者阅读到的文章中不存在二手的审阅、辩论、修改痕迹，如果存在此类痕迹，说明你的修改是失败的。示例：
  - 原文：其中，$\langle \chi_i \| \chi_j \rangle$是FC因子，我们在第3节已经介绍过，它与两个态之间的能量差相关。两个态之间的能量差越小，FC因子就越大，有利于非辐射跃迁。由此可知：能量差越小，$k_{\text{nr}}$通常越大。
      - 错误修改：其中，$\langle \chi_i \| \chi_j \rangle$严格来说是核振动波函数的重叠振幅，它的模平方才是Franck-Condon因子。能隙定律不能简单解释为电子态能量差越小，单个FC因子越大：FC因子由核振动波函数的重叠决定，而非辐射跃迁中的能隙效应还涉及能量守恒条件下可接受的末态振动态、态密度以及非绝热耦合等因素。电子态能隙减小时，体系通常更容易找到能量匹配且具有有效耦合的末态振动态，因此$k_{\text{nr}}$常会增大，但实际速率还同时取决于振动模式、重组能和电子态耦合强度。
      - 正确修改：其中，$\langle \chi_i \| \chi_j \rangle$是核振动波函数的重叠振幅，其平方是FC因子。我们在第3节已经介绍过，FC因子与两个态之间的能量差相关。当电子态能隙减小时，体系通常更容易找到能量匹配且具有有效耦合的末态振动态，因此$k_{\text{nr}}$常会增大。不过，实际速率还同时取决于振动模式、重组能和电子态耦合强度。

## latex
- 行内公式使用`$`引用，行间公式使用`$$`引用
- 尽量把同一个公式写在一行，不要拆分
- 行间公式前后都需要换行

## Delivery
- 如果项目维护者要求你修改源码/文章，则在交付时，patch应当采用Git unified diff format，且需要在项目根目录直接git apply可以应用成功

## others
- 同一行出现复数个|时，需要使用\进行转义

